meta {
  name: Add Member
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/projects/{{projectId}}/members
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "userId": "{{userId}}"
  }
}
