meta {
  name: Create Project (v2)
  type: http
  seq: 2
}

post {
  url: {{baseUrlV2}}/projects
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Projet v2",
    "description": "Créé via l'API v2"
  }
}
